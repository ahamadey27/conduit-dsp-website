# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Conduit DSP website — a static website. Currently in early development with a single `index.html` file.

## Development

No build tools, package manager, or test framework configured yet. Open `index.html` directly in a browser to preview.
